name: ADC web application deployment
run-name: Deploy ADC web application to ${{ inputs.deploy_target }} [@${{ github.actor }}]

on:
  workflow_dispatch:

jobs:
  deploy-production:
    name: deploy
    uses: ./.github/workflows/deploy.yml
    with:
      environment: 'production'
