name: ADC web application deployment [PRODUCTION]
run-name: Deploy ADC web application to PRODUCTION [@${{ github.actor }}]

on:
  workflow_dispatch:

jobs:
  deploy-production:
    name: deploy
    uses: ./.github/workflows/deploy.yml
    secrets: inherit
    with:
      environment: 'production'
