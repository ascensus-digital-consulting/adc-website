name: Website release [production]
run-name: ${{ github.actor }} is deploying the ADC web application to production
on:
  workflow_dispatch:
  push:
    branches:
      - main
      
jobs:
  measure-code:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment: production
    
    steps:
      - name: Gather metrics
        run: echo "TODO - implement code metrics via SonarQube"

  test-code:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment: production
    
    steps:
      - name: Test code
        run: echo "TODO - implement code testing via Jest"

  test-security:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment: production
    
    steps:
      - name: Test application security
        run: echo "TODO - implement security testing via Snyk (maybe?)"

  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment: production
    needs: [measure-code, test-code, test-security]
    
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: 23.4.0
          
      - name: Install dependencies
        run: npm install
        
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-west-1
          
      - name: Deploy application
        uses: reggionick/s3-deploy@v4
        with:
          folder: app/src
          bucket: ${{ secrets.S3_BUCKET }}
          bucket-region: ${{ secrets.S3_BUCKET_REGION }}
          dist-id: ${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }}
          invalidation: /
          delete-removed: true
          no-cache: true
          private: true
          files-to-include: '{.*/**,**}'
  
  smoke-test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment: production
    needs: deploy
    
    steps:
      - name: Smoke test
        run: echo "TODO - implement smoke testing. Tech stack decision pending..."
